A simple display of the most recently deployed Heroku app.

Add it via HTTP deploy hook:

```bash
$ heroku addons:create deployhooks:http --url=http://example.org
```

## Running

```bash
$ npm run build_public
$ npm start
```
